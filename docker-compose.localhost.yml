version: '3'

services:
  app:
    image: node:alpine
    volumes:
      - ./:/app
    working_dir: /app
    depends_on:
      - db
    environment:
      PGHOST: db
    ports:
      - ${NODE_PORT}:${NODE_PORT}
    command: npm run dev

  db:
    image: postgres:12-alpine
    environment:
      PGUSER: ${PGUSER}
    expose:
      - ${PGPORT}
    volumes:
      - data:/var/lib/postgresql/data

volumes:
  data: {}
