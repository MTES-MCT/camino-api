mutation Activite($activite: InputActiviteModification!, $deposer: Boolean) {
  activiteModifier(activite: $activite, deposer: $deposer) {
    ...Activite
  }
}

fragment Activite on Activite {
  id
  date
  contenu
}
