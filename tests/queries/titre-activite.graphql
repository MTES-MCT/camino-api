mutation Activite($activite: InputActiviteModification!, $depose: Boolean) {
  activiteModifier(activite: $activite, depose: $depose) {
    ...Activite
  }
}

fragment Activite on Activite {
  id
  date
  contenu
}
