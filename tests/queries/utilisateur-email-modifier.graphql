mutation UtilisateurEmailModifier($emailToken: String!) {
  utilisateurEmailModifier(emailToken: $emailToken) {
    utilisateur {
      id
      email
      nom
      prenom
    }
    accessToken
    refreshToken
  }
}
