type Query {
  metas: Metas

  "Un titre"
  titre(id: ID!): Titre

  "Liste de titres"
  titres(
    typeIds: [TypeId]
    domaineIds: [DomaineId]
    statutIds: [StatutId]
    substances: [String]
    noms: [String]
  ): [Titre]

  "Un utilisateur"
  utilisateur(id: ID!): Utilisateur

  "Liste des utilisateurs"
  utilisateurs(
    entrepriseIds: [ID]
    administrationIds: [ID]
    noms: [String]
  ): [Utilisateur]

  substance(id: String!): Substance

  substances: [Substance]

  utilisateurIdentifier: Utilisateur

  permissions: [Permission]

  entreprise(id: String!): Entreprise

  entreprises: [Entreprise]
}

type Mutation {
  titreAjouter(titre: InputTitre!): Titre

  titreModifier(titre: InputTitre!): Titre

  titreSupprimer(id: ID!): Titre

  utilisateurConnecter(id: ID!, motDePasse: String!): UtilisateurToken

  utilisateurAjouter(utilisateur: InputUtilisateur): Utilisateur

  utilisateurModifier(utilisateur: InputUtilisateur): Utilisateur
}

scalar json
