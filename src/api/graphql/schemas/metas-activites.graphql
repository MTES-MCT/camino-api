# import * from 'scalars.graphql'
# import * from 'territoires.graphql'
# import * from 'calendrier.graphql'

type ActiviteStatut {
  id: ID!
  nom: String!
  couleur: String!
}

type ActiviteType {
  id: ID!
  nom: String!
  sections: Json!
  frequence: Frequence!
  pays: [Pays!]
}
