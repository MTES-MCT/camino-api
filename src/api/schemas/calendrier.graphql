type Frequence {
  id: ID!
  nom: String!
  annees: [Annee]
  trimestres: [Trimestre]
  mois: [Mois]
}

type Trimestre {
  id: ID!
  nom: String!
  mois: [Mois]
}

type Mois {
  id: ID!
  nom: String!
}

type Annee {
  id: ID!
  nom: String!
}
