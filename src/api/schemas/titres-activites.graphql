type Activite {
  id: ID!
  type: ActiviteType!
  titre: Titre
  statut: ActiviteStatut!
  date: String!
  dateSaisie: String
  periode: Json
  annee: Int
  sections: Json
  contenu: Json
  frequencePeriodeId: Int

  modification: Boolean
}

input InputActiviteModification {
  id: ID!
  statutId: String!
  contenu: Json
}
