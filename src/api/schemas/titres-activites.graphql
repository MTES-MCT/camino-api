type Activite {
  id: ID!
  type: ActiviteType!
  titre: Titre
  statut: ActiviteStatut!
  date: String!
  dateSaisie: String
  periode: Json
  annee: Int
  sections: Json
  contenu: Json
  frequencePeriodeId: Int

  editable: Boolean
}

input InputActiviteModification {
  id: ID!
  activiteStatutId: String!
  contenu: Json
}
