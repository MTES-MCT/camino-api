type Activite {
  id: ID!
  type: ActiviteType!
  statut: ActiviteStatut!
  date: String!
  dateSaisie: String
  periode: Json
  annee: Int
  sections: Json
  contenu: Json
}

input InputActiviteModification {
  id: ID!
  activiteStatutId: String!
  contenu: Json
}
